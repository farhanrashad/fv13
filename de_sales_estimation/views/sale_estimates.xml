<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!--        Qutaions Smart Button Action Window    -->
        <record model="ir.actions.act_window" id="open_qute_estimates">
            <field name="name">Qutations</field>
            <field name="res_model">sales.estimate</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[('partner_id','=','active_id')]</field>
            <field name="context">{}</field>
        </record>

        <!--        Action for Estimate Smart Button    -->
        <record model="ir.actions.act_window" id="open_sale_estimates">
            <field name="name">Estimate</field>
            <field name="res_model">sales.estimate</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[('partner_id','=','active_id')]</field>
            <field name="context">{}</field>
        </record>

        <!--        Smart Button in CRM Of Estimate -->
        <record id="sale_crm_smart_btton" model="ir.ui.view">
            <field name="name">sale.smart.button</field>
            <field name="model">crm.lead</field>
            <field name="inherit_id" ref="crm.crm_lead_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//form//sheet//div[@name='button_box']"
                       position="inside">
                    <button name="%(open_sale_estimates)d" type="action" class="oe_stat_button"
                            icon="fa-usd">
                        <field name="estimate_count_id" string="Estimate" widget="statinfo"/>
                    </button>
                </xpath>
            </field>
        </record>

        <!--       Sales Estimates form View    -->
        <record id="sale_estimates_form" model="ir.ui.view">
            <field name="name">sales.estimate.form</field>
            <field name="model">sales.estimate</field>
            <field name="arch" type="xml">
                <form string="">
                    <header>
                        <button name="action_confirm" id="action_confirm" states="draft"
                                string="Confirm" class="btn-primary" type="object"/>
                        <button name="create_qute" string="Create Qutation" type="object" class="btn-primary"
                                attrs="{'invisible':['|','|',('state', 'not in', 'done'),('state', 'not in', 'done')]}"/>

                        <button name="action_quotation_estimate" string="Send by Email" type="object"
                                class="btn-primary"/>
                        <button name="action_cancel" type="object" string="Cancel"
                                attrs="{'invisible': ['|','|',('state', 'in', 'done'),('state', 'in', 'create')]}"/>
                        <button name="reset" string="Reset to Draft" type="object" class="btn-primary"
                                attrs="{'invisible': ['|','|',('state', 'not in', 'done'),('state', 'in', 'create')]}"
                        />
                        <field name="state" widget="statusbar" statusbar_visible=""
                               statusbar_colors="draft,sent,sale"/>
                    </header>
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="open_qute_estimates" type="object" class="oe_stat_button"
                                    icon="fa-pencil-square-o">
                                <field name="qute_count_id" string="Qutation" widget="statinfo"/>
                            </button>
                        </div>
                        <div class="oe_title">
                            <h1>
                                <field name="name_seq" readonly="1"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="partner_id"/>
                                <field name="payment_terms_id"/>
                                <field name="client_ref"/>
                            </group>
                            <group>
                                <field name="est_date"/>
                                <field name="est_src"/>
                                <field name="est_user_id"/>
                                <field name="est_team"/>
                                <field name="sale_qute"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Estimation Lines">
                                <field name="est_order_line">
                                    <tree editable="bottom">
                                        <field name="product_id"/>
                                        <field name="est_name"/>
                                        <field name="product_uom_qty"/>
                                        <field name="product_uom_id"/>
                                        <field name="price_unit"/>
                                        <field name="discount"/>
                                        <field name="currency_id" invisible="True"/>
                                        <field name="price_subtotal" widget="monetary"
                                               options="{'currency_field': 'currency_id'}"/>
                                    </tree>
                                </field>
                                <!--                               group for subtotal-->
                                <group class="oe_subtotal_footer oe_right" colspan="2" name="sale_total">
                                    <!--                                    <div class="oe_subtotal_footer_separator oe_inline o_td_label">-->
                                    <label for="amount_total"/>
                                    <field name="amount_total" nolabel="1"
                                           widget='monetary' options="{'currency_field': 'currency_id'}"/>

                                    <!--                                    </div>-->
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!--        Sales Estimates Tree View      -->
        <record id="sale_estimates_tree" model="ir.ui.view">
            <field name="name">sale.estimates.tree</field>
            <field name="model">sales.estimate</field>
            <field name="arch" type="xml">
                <tree string="">
                    <field name="name_seq" readonly="1"/>
                    <field name="partner_id"/>
                    <field name="est_date"/>
                    <field name="amount_total"/>
                    <field name="est_user_id"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>

        <!--   action for Estimates     -->
        <act_window id="sale_estimates_action"
                    name="Estimates"
                    res_model="sales.estimate"
                    view_mode="tree,form"
        />

        <!--   action for CRM Estimates     -->
        <act_window id="crm_estimates_action"
                    name="Estimates"
                    res_model="sales.estimate"
                    view_mode="tree,form"
        />

        <!--        menu name estimates in sale order     -->
        <menuitem id="sale_estimates_menu" name="Estimates" parent="sale.sale_order_menu" action="sale_estimates_action"
                  sequence="3"/>

        <!--        menu for crm estimates-->
        <menuitem id="crm_estimates_menu" name="Estimates" parent="crm.crm_menu_sales" action="crm_estimates_action"
                  sequence="3"/>
    </data>
</odoo>